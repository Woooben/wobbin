  function reset(){S.query='';S.platform='All';S.category='All';S.app='All';S.element='All';S.collection=null;S.nav='discover';render()}
  async function toggleFav(x){x.favorite=!x.favorite;if(!x.demo){await dbPut({...x,imageUrl:undefined});}render()}
  function bindDetail(){const x=S.selected,close=()=>{S.selected=null;render()};document.getElementById('detail-close').onclick=close;document.getElementById('detail-bg').onmousedown=e=>{if(e.target.id==='detail-bg')close()};document.getElementById('detail-fav').onclick=()=>toggleFav(x);document.getElementById('delete')?.addEventListener('click',async()=>{if(!confirm(`确定删除「${x.title}」吗？`))return;await dbDelete(x.id);S.items=S.items.filter(i=>i.id!==x.id);S.selected=null;render();toast('已删除')});document.getElementById('download').onclick=()=>{const a=document.createElement('a');a.href=x.imageUrl;a.download=`${x.app}-${x.title}`;a.click()}}

  function bindUpload(){let entries=[],manual=new Set();const drop=document.getElementById('drop'),zip=document.getElementById('zip-input'),folder=document.getElementById('folder-input'),images=document.getElementById('image-input'),summary=document.getElementById('file-summary'),preview=document.getElementById('package-preview');document.querySelectorAll('[data-upload-mode]').forEach(b=>b.onclick=()=>{S.uploadMode=b.dataset.uploadMode;render()});document.querySelectorAll('[data-manual-element]').forEach(b=>b.onclick=()=>{const e=b.dataset.manualElement;manual.has(e)?manual.delete(e):manual.add(e);b.classList.toggle('active',manual.has(e))});const clickInput=()=>({zip,folder,images}[S.uploadMode]||images).click();drop.onclick=clickInput;zip.onchange=async e=>{entries=await readInputFiles(e.target.files,'zip');showEntries()};folder.onchange=async e=>{entries=await readInputFiles(e.target.files,'folder');showEntries()};images.onchange=async e=>{entries=await readInputFiles(e.target.files,'images');showEntries()};drop.ondragover=e=>e.preventDefault();drop.ondrop=async e=>{e.preventDefault();entries=await readInputFiles(e.dataTransfer.files,'auto');showEntries()};function showEntries(){drop.classList.toggle('ready',!!entries.length);summary.textContent=entries.length?`已识别 ${entries.length} 张截图`:'没有识别到可用图片';preview.classList.toggle('hidden',!entries.length);if(entries.length){const roots=unique(entries.map(x=>x.path.split('/')[0])).slice(0,5);preview.innerHTML=`<strong>文件结构预览</strong><br>${entries.slice(0,6).map(x=>esc(x.path)).join('<br>')}${entries.length>6?`<br>…共 ${entries.length} 张`:''}<br><span>识别到一级目录：${roots.map(esc).join('、')}</span>`}}const close=()=>{S.upload=false;render()};document.getElementById('upload-close').onclick=close;document.getElementById('upload-cancel').onclick=close;document.getElementById('upload-bg').onmousedown=e=>{if(e.target.id==='upload-bg')close()};document.getElementById('upload-submit').onclick=async()=>{if(!entries.length)return toast('请先选择 ZIP、文件夹或图片');const btn=document.getElementById('upload-submit');btn.disabled=true;const defaults={app:val('up-app').trim(),platform:val('up-platform'),flow:val('up-flow').trim(),category:val('up-category')};try{for(let i=0;i<entries.length;i++){btn.textContent=`正在导入 ${i+1}/${entries.length}`;const item=makeItem(entries[i],defaults,[...manual]);await dbPut({...item,imageUrl:undefined});S.items.unshift(hydrate(item))}S.upload=false;S.nav='discover';S.collection=null;render();toast(`已导入 ${entries.length} 张截图`)}catch(e){btn.disabled=false;btn.textContent='开始导入';toast(e.message||'导入失败')}}}

  async function readInputFiles(fileList,mode){const files=[...fileList];if(!files.length)return[];const zips=files.filter(f=>f.name.toLowerCase().endsWith('.zip'));if((mode==='zip'||mode==='auto')&&zips.length){if(!window.JSZip)throw new Error('ZIP 组件加载失败，请检查网络');const out=[];for(const zf of zips){const z=await JSZip.loadAsync(zf);for(const [path,entry] of Object.entries(z.files)){if(entry.dir||!isImage(path)||path.includes('__MACOSX'))continue;const blob=await entry.async('blob');out.push({file:new File([blob],path.split('/').pop(),{type:mime(path)}),path:normalizePath(path)})}}return stripCommonRoot(out)}return stripCommonRoot(files.filter(f=>isImage(f.name)).map(f=>({file:f,path:normalizePath(f.webkitRelativePath||f.name)})))}
  function stripCommonRoot(entries){if(entries.length<2)return entries;const roots=entries.map(x=>x.path.split('/')[0]);if(roots.every(r=>r===roots[0])&&entries.every(x=>x.path.includes('/')))return entries.map(x=>({...x,path:x.path.split('/').slice(1).join('/')}));return entries}
  function isImage(n){return /\.(png|jpe?g|webp|gif)$/i.test(n)}function mime(n){const e=n.split('.').pop().toLowerCase();return e==='png'?'image/png':e==='webp'?'image/webp':e==='gif'?'image/gif':'image/jpeg'}function normalizePath(p){return String(p||'').replace(/^\.\//,'').replace(/\\/g,'/').replace(/\/+/g,'/')}
  function makeItem(entry,d,manual){const path=entry.path,parts=path.split('/').filter(Boolean),fileName=parts.pop()||entry.file.name,text=[path,d.app,d.flow].join(' ');const app=d.app||parts[0]||cleanName(fileName);const flow=d.flow||parts.slice(1).join(' / ')||parts[0]||'Imported screens';const category=d.category&&d.category!=='自动识别'?d.category:inferCategory(text);const elements=unique([...inferElements(text),...manual]);return{id:crypto.randomUUID(),title:cleanName(fileName),app,flow,platform:inferPlatform(text,d.platform),category,collection:app,elements:elements.length?elements:['Cards'],tags:unique(parts),blob:entry.file,imageUrl:URL.createObjectURL(entry.file),favorite:false,createdAt:new Date().toISOString(),sourcePath:path,demo:false}}
  const val=id=>document.getElementById(id)?.value||'';let toastTimer;function toast(msg){const el=document.getElementById('toast');el.textContent=msg;el.classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>el.classList.remove('show'),2800)}
  window.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();document.getElementById('search')?.focus()}if(e.key==='Escape'&&(S.selected||S.upload)){S.selected=null;S.upload=false;render()}});
  render();init();
})();
