function header(){return `<header class="topbar"><div class="brand"><button class="brand-logo" id="home-logo" aria-label="Wobbin"><img src="${LOGO}" alt="Wobbin"></button><nav class="brand-nav"><button class="active" id="home-nav">Apps</button><button disabled>Sites</button></nav></div><label class="searchbar">${searchIcon}<input id="global-search" value="${attr(S.query)}" placeholder="搜索 App、页面或 UI 元素…" autocomplete="off"><span class="search-shortcut">⌘ K</span></label><div class="actions"><button id="theme" class="icon-btn" title="切换主题">${moonIcon}</button><button id="import" class="import-btn">导入</button></div></header>`}
function quickElements(){return `<div class="quick-elements"><button class="element-chip ${S.element==='All'?'active':''}" data-element="All">全部元素</button>${ELEMENTS.map(([k,l])=>`<button class="element-chip ${S.element===k?'active':''}" data-element="${k}">${l}</button>`).join('')}</div>`}
function platform(){return `<div class="platform-toggle">${['All','iOS','Android','Web'].map(p=>`<button class="${S.platform===p?'active':''}" data-platform="${p}">${p==='All'?'全部':p}</button>`).join('')}</div>`}
function home(){const aa=apps();return `${header()}<main class="main"><div class="subnav"><div class="subnav-left">${platform()}<span class="divider"></span><span class="page-label"><strong>Apps</strong> · 我的产品截图库</span></div><button class="filter-btn">${filterIcon} Filter</button></div>${quickElements()}<div class="section-head"><div><h1>Apps</h1><p>所有产品文件包，点击后查看该 App 的全部截图。</p></div><span class="section-count">${aa.length} apps</span></div>${aa.length?`<section class="apps-grid">${aa.map(appCard).join('')}</section>`:`<div class="empty"><strong>没有找到 App</strong>调整筛选条件，或导入新的截图文件包。</div>`}</main>${suggestions()}`}
function appCard(a){const c=appCover(a),web=c?.platform==='Web';return `<article class="app-card"><div class="cover-wrap" data-open-app="${attr(a.name)}"><div class="cover-image ${web?'web':''}"><img src="${attr(c?.imageUrl||'')}" alt="${attr(a.name)}"></div><div class="card-actions"><button data-cover-app="${attr(a.name)}">设置封面</button>${a.items.some(x=>!x.demo)?`<button class="danger" data-delete-app="${attr(a.name)}">删除</button>`:''}</div></div><div class="card-info"><div class="app-icon">${esc(a.name.slice(0,1).toUpperCase())}</div><div class="app-copy"><strong>${esc(a.name)}</strong><span>${a.items.length} screens · ${a.flows} flows · ${a.platforms.join(' / ')}</span></div></div></article>`}
function detail(){const list=currentItems(),a=all().filter(x=>x.app===S.app),platforms=unique(a.map(x=>x.platform));return `${header()}<main class="main"><button id="back" class="back">${backIcon} Apps</button><div class="app-head"><div class="app-title"><div class="app-icon">${esc((S.app||'?').slice(0,1).toUpperCase())}</div><div><h1>${esc(S.app)}</h1><p>${a.length} screens · ${unique(a.map(x=>x.flow)).length} flows · ${platforms.join(' / ')}</p></div></div><button class="import-btn" id="set-cover-current">设置封面</button></div><div class="tabs"><button data-tab="screens" class="${S.tab==='screens'?'active':''}">Screens</button><button data-tab="flows" class="${S.tab==='flows'?'active':''}">Flows</button><button data-tab="elements" class="${S.tab==='elements'?'active':''}">UI Elements</button></div><div class="subnav"><div class="subnav-left">${platform()}<span class="divider"></span><span class="page-label">${S.element==='All'?'全部元素':ELEMENTS.find(e=>e[0]===S.element)?.[1]||S.element}</span></div><button class="filter-btn">${filterIcon} Filter</button></div>${quickElements()}${S.tab==='flows'?flowView(list):screenView(list)}</main>${suggestions()}`}
function screenView(list){return list.length?`<section class="screens-grid">${list.map(screenCard).join('')}</section>`:`<div class="empty"><strong>没有对应截图</strong>调整平台、元素或搜索条件。</div>`}function screenCard(x){return `<article class="screen-card"><div class="screen-frame ${x.platform==='Web'?'web':''}" data-screen="${x.id}"><img src="${attr(x.imageUrl)}" alt="${attr(x.title)}"></div><div class="screen-name"><strong>${esc(x.title)}</strong><span>${esc(x.flow||'Screen')} · ${(x.elements||[]).slice(0,2).map(e=>ELEMENTS.find(v=>v[0]===e)?.[1]||e).join(' · ')}</span></div></article>`}function flowView(list){const m=new Map();for(const x of list){const k=x.flow||'Imported screens';if(!m.has(k))m.set(k,[]);m.get(k).push(x)}return m.size?`<div style="display:grid;gap:32px">${[...m].map(([n,ss])=>`<section><div class="section-head"><div><h1 style="font-size:17px">${esc(n)}</h1><p>${ss.length} screens</p></div></div><div class="screens-grid">${ss.map(screenCard).join('')}</div></section>`).join('')}</div>`:`<div class="empty">暂无 Flow</div>`}
function suggestions(){if(!S.query.trim())return'';const q=S.query.trim().toLowerCase(),els=ELEMENTS.filter(([k,l])=>k.toLowerCase().includes(q)||l.includes(S.query.trim())).slice(0,6);if(!els.length)return'';return `<div class="search-suggest"><div class="suggest-label">快捷元素搜索</div>${els.map(([k,l])=>`<button class="suggest-item" data-suggest-element="${k}"><span>${l}</span><em>UI Element</em></button>`).join('')}</div>`}