  function allItems(){return [...S.items,...DEMO]}
  function apps(){return ['All',...unique(allItems().map(x=>x.app)).sort()]}
  function categories(){return ['All',...unique(allItems().map(x=>x.category)).sort()]}
  function collections(){const m=new Map();allItems().forEach(x=>m.set(x.collection,(m.get(x.collection)||0)+1));return [...m.entries()].sort((a,b)=>b[1]-a[1])}
  function elementCounts(){const m={};allItems().forEach(x=>(x.elements||[]).forEach(e=>m[e]=(m[e]||0)+1));return m}
  function filtered(){const q=slug(S.query);return allItems().filter(x=>{const text=slug([x.title,x.app,x.flow,x.platform,x.category,x.collection,...(x.elements||[]),...(x.tags||[])].join(' '));return(!q||text.includes(q))&&(S.platform==='All'||x.platform===S.platform)&&(S.category==='All'||x.category===S.category)&&(S.app==='All'||x.app===S.app)&&(S.element==='All'||(x.elements||[]).includes(S.element))&&(S.nav!=='favorites'||x.favorite)&&(!S.collection||x.collection===S.collection)}).sort((a,b)=>{if(S.sort==='name')return a.app.localeCompare(b.app);const d=new Date(a.createdAt)-new Date(b.createdAt);return S.sort==='oldest'?d:-d})}
  function groupedFlows(){const map=new Map();filtered().forEach(x=>{const key=`${x.app}::${x.flow}`;if(!map.has(key))map.set(key,{app:x.app,flow:x.flow,items:[]});map.get(key).items.push(x)});return [...map.values()]}
  function pageTitle(){if(S.collection)return S.collection;if(S.nav==='favorites')return'Favorites';if(S.nav==='collections')return'Collections';return'Discover'}
  function select(id,label,value,opts,labels={}){return`<label class="select-chip"><span>${label}</span><select id="${id}">${opts.map(o=>`<option value="${attr(o)}" ${value===o?'selected':''}>${esc(labels[o]||(o==='All'?'全部':o))}</option>`).join('')}</select></label>`}
  function nav(id,ico,label,active){return`<button data-nav="${id}" class="${active?'active':''}">${icon(ico,id==='favorites'&&active)}<span>${label}</span></button>`}
  function card(x){return`<article class="card"><div class="preview" data-open="${x.id}" role="button" tabindex="0"><div class="preview-box ${x.platform==='Web'?'web':''}"><img src="${attr(x.imageUrl)}" alt="${attr(x.title)}" loading="lazy"></div><span class="badge">${esc(x.platform)}</span><button class="fav-float ${x.favorite?'active':''}" data-fav="${x.id}" aria-label="收藏">${icon('heart',x.favorite)}</button></div><div class="meta"><div class="avatar">${esc(x.app.slice(0,1).toUpperCase())}</div><div class="copy"><strong>${esc(x.app)}</strong><span>${esc(x.title)}</span></div></div><div class="mini-tags">${(x.elements||[]).slice(0,3).map(e=>`<span>${esc(ELEMENT_LABELS[e]||e)}</span>`).join('')}</div></article>`}
  function flowView(){const flows=groupedFlows();return flows.length?`<section class="flow-list">${flows.map(f=>`<article class="flow"><div class="flow-head"><div class="flow-title"><div class="avatar">${esc(f.app.slice(0,1).toUpperCase())}</div><div><strong>${esc(f.flow)}</strong><span>${esc(f.app)}</span></div></div><em>${f.items.length} screens</em></div><div class="flow-shots">${f.items.map(x=>`<button class="flow-shot" data-open="${x.id}"><img src="${attr(x.imageUrl)}" alt="${attr(x.title)}"><span>${esc(x.title)}</span></button>`).join('')}</div></article>`).join('')}</section>`:emptyView()}
  function emptyView(){return`<section class="empty"><div class="big-icon">${icon('grid')}</div><h3>没有找到对应内容</h3><p>调整筛选条件，或者导入新的截图文件包。</p><div class="empty-actions"><button id="empty-reset" class="secondary">重置筛选</button><button id="empty-upload" class="primary">导入文件包</button></div></section>`}
  function appTemplate(){const items=filtered(),counts=elementCounts();return`<div class="shell">${S.side?'<button id="overlay" class="overlay"></button>':''}<aside class="sidebar ${S.side?'open':''}"><div class="brand"><span class="logo">${icon('spark')}</span><div><strong>ScreenVault</strong><span>UI reference library</span></div><button id="close-side" class="icon-btn close-side">${icon('close')}</button></div><nav class="nav">${nav('discover','spark','发现',S.nav==='discover'&&!S.collection)}${nav('screens','grid','Screens',S.nav==='screens')}${nav('elements','layers','UI Elements',S.nav==='elements')}${nav('flows','flow','Flows',S.nav==='flows')}${nav('favorites','heart','收藏',S.nav==='favorites')}</nav><div class="section-label"><span>Collections</span><span>•••</span></div><div class="collections">${collections().map(([n,c])=>`<button class="collection-btn ${S.collection===n?'active':''}" data-col="${attr(n)}">${icon('folder')}<span>${esc(n)}</span><em>${c}</em></button>`).join('')}</div><div class="sidebar-footer"><div class="storage-card">${icon('cloud')}<div><strong>浏览器资料库</strong><span>截图会保存在当前浏览器。网站本身可在任何电脑打开。</span></div></div>${S.items.length?'<button id="clear-local" class="clear-btn">清空本机导入内容</button>':''}</div></aside><main class="main"><header class="topbar"><button id="menu" class="icon-btn mobile-menu">${icon('menu')}</button><label class="search">${icon('search')}<input id="search" value="${attr(S.query)}" placeholder="搜索 App、流程、页面、元素…"><kbd>⌘ K</kbd></label><button id="open-upload" class="primary">${icon('upload')}<span>导入文件包</span></button></header><section class="hero"><div><small>DESIGN REFERENCE LIBRARY</small><h1>${esc(pageTitle())}</h1><p>像 Mobbin 一样按 Screens、UI Elements 和 Flows 浏览自己的产品截图。</p></div><span class="count">${items.length} screens</span></section><div class="content-tabs"><button class="content-tab ${S.content==='screens'?'active':''}" data-content="screens">Screens</button><button class="content-tab ${S.content==='elements'?'active':''}" data-content="elements">UI Elements</button><button class="content-tab ${S.content==='flows'?'active':''}" data-content="flows">Flows</button></div><section class="filter-panel"><div class="filter-row">${select('platform','平台',S.platform,['All','iOS','Android','Web'])}${select('app-filter','App',S.app,apps())}${select('category','页面类型',S.category,categories())}${select('sort','排序',S.sort,['newest','oldest','name'],{newest:'最新导入',oldest:'最早导入',name:'App 名称'})}${(S.query||S.platform!=='All'||S.category!=='All'||S.app!=='All'||S.element!=='All'||S.collection||S.nav==='favorites')?'<button id="reset" class="reset">清除筛选</button>':''}</div><div class="element-strip"><button class="element-chip ${S.element==='All'?'active':''}" data-element="All">全部元素 <em>${allItems().length}</em></button>${ELEMENTS.map(e=>`<button class="element-chip ${S.element===e?'active':''}" data-element="${attr(e)}">${esc(ELEMENT_LABELS[e])} <em>${counts[e]||0}</em></button>`).join('')}</div></section>${S.loading?'<section class="empty"><p>正在载入资料库…</p></section>':S.content==='flows'?flowView():items.length?`<section class="grid">${items.map(card).join('')}</section>`:emptyView()}</main>${S.selected?detailTemplate(S.selected):''}${S.upload?uploadTemplate():''}</div>`}
  function detailTemplate(x){return`<div id="detail-bg" class="backdrop"><section class="detail"><header class="detail-bar"><div><span>${esc(x.app)} · ${esc(x.platform)} · ${esc(x.flow)}</span><strong>${esc(x.title)}</strong></div><div class="detail-actions"><button id="detail-fav" class="icon-btn ${x.favorite?'active':''}">${icon('heart',x.favorite)}</button><button id="download" class="icon-btn">${icon('down')}</button>${!x.demo?`<button id="delete" class="icon-btn danger">${icon('trash')}</button>`:''}<button id="detail-close" class="icon-btn">${icon('close')}</button></div></header><div class="detail-body"><div class="detail-image"><img src="${attr(x.imageUrl)}" alt="${attr(x.title)}"></div><aside class="detail-info">${info('App',x.app)}${info('Flow',x.flow)}${info('平台',x.platform)}${info('页面类型',x.category)}${info('集合',x.collection)}<div style="padding-top:17px"><span style="color:#93938c;font-size:10px">UI Elements</span><div class="detail-elements">${(x.elements||[]).map(e=>`<em>${esc(ELEMENT_LABELS[e]||e)}</em>`).join('')||'<small>暂无元素标签</small>'}</div></div></aside></div></section></div>`}
  function info(k,v){return`<div class="info"><span>${k}</span><strong>${esc(v||'—')}</strong></div>`}
  function uploadTemplate(){return`<div id="upload-bg" class="backdrop"><section class="upload-modal"><header class="upload-head"><div><small>IMPORT REFERENCES</small><h2>导入截图文件包</h2></div><button id="upload-close" class="icon-btn">${icon('close')}</button></header><div class="upload-tabs"><button class="upload-tab ${S.uploadMode==='zip'?'active':''}" data-upload-mode="zip">ZIP 压缩包</button><button class="upload-tab ${S.uploadMode==='folder'?'active':''}" data-upload-mode="folder">整个文件夹</button><button class="upload-tab ${S.uploadMode==='images'?'active':''}" data-upload-mode="images">多张图片</button></div><button id="drop" class="drop"><span class="upload-icon">${icon('upload')}</span><strong id="drop-title">${S.uploadMode==='zip'?'选择或拖入 ZIP 压缩包':S.uploadMode==='folder'?'选择包含截图的整个文件夹':'选择或拖入多张截图'}</strong><span id="file-summary">${S.uploadMode==='zip'?'自动解压并读取 PNG、JPG、WebP':S.uploadMode==='folder'?'会保留文件夹层级并生成 App / Flow':'支持 PNG、JPG、WebP，可批量选择'}</span><input id="zip-input" type="file" accept=".zip,application/zip"><input id="folder-input" type="file" webkitdirectory directory multiple><input id="image-input" type="file" multiple accept="image/png,image/jpeg,image/webp,image/gif"></button><div id="package-preview" class="package-preview hidden"></div><div class="form-grid"><div class="field"><span>默认 App 名称</span><input id="up-app" placeholder="留空则使用一级文件夹名"></div><div class="field"><span>默认平台</span><select id="up-platform"><option>iOS</option><option>Android</option><option>Web</option></select></div><div class="field"><span>默认 Flow</span><input id="up-flow" placeholder="留空则使用二级文件夹名"></div><div class="field"><span>默认页面类型</span><select id="up-category"><option>自动识别</option>${Object.keys(CATEGORY_KEYS).map(x=>`<option>${x}</option>`).join('')}</select></div><div class="field wide"><span>统一补充元素标签</span><div class="manual-elements">${ELEMENTS.map(e=>`<button type="button" data-manual-element="${attr(e)}">${esc(ELEMENT_LABELS[e])}</button>`).join('')}</div></div></div><footer class="upload-foot"><span class="upload-note">目录建议：App / Flow / screenshot.png</span><div class="foot-actions"><button id="upload-cancel" class="secondary">取消</button><button id="upload-submit" class="primary">开始导入</button></div></footer></section></div>`}

  function render(){document.getElementById('app').innerHTML=appTemplate();bind()}
  function bind(){document.querySelectorAll('[data-nav]').forEach(b=>b.onclick=()=>{S.nav=b.dataset.nav;S.collection=null;if(['screens','elements','flows'].includes(S.nav))S.content=S.nav==='elements'?'elements':S.nav;S.side=false;render()});document.querySelectorAll('[data-col]').forEach(b=>b.onclick=()=>{S.collection=b.dataset.col;S.nav='collections';S.side=false;render()});document.querySelectorAll('[data-content]').forEach(b=>b.onclick=()=>{S.content=b.dataset.content;render()});document.querySelectorAll('[data-element]').forEach(b=>b.onclick=()=>{S.element=b.dataset.element;S.content='elements';render()});document.querySelectorAll('[data-open]').forEach(b=>b.onclick=()=>{S.selected=allItems().find(x=>x.id===b.dataset.open);render()});document.querySelectorAll('[data-fav]').forEach(b=>b.onclick=async e=>{e.stopPropagation();const x=allItems().find(v=>v.id===b.dataset.fav);await toggleFav(x)});const q=document.getElementById('search');if(q)q.oninput=e=>{S.query=e.target.value;const pos=e.target.selectionStart;render();const n=document.getElementById('search');n.focus();n.setSelectionRange(pos,pos)};document.getElementById('platform')?.addEventListener('change',e=>{S.platform=e.target.value;render()});document.getElementById('app-filter')?.addEventListener('change',e=>{S.app=e.target.value;render()});document.getElementById('category')?.addEventListener('change',e=>{S.category=e.target.value;render()});document.getElementById('sort')?.addEventListener('change',e=>{S.sort=e.target.value;render()});document.getElementById('reset')?.addEventListener('click',reset);document.getElementById('empty-reset')?.addEventListener('click',reset);document.getElementById('open-upload')?.addEventListener('click',()=>{S.upload=true;render()});document.getElementById('empty-upload')?.addEventListener('click',()=>{S.upload=true;render()});document.getElementById('menu')?.addEventListener('click',()=>{S.side=true;render()});document.getElementById('close-side')?.addEventListener('click',()=>{S.side=false;render()});document.getElementById('overlay')?.addEventListener('click',()=>{S.side=false;render()});document.getElementById('clear-local')?.addEventListener('click',async()=>{if(confirm('确定清空本机导入的全部截图吗？')){await dbClear();S.items=[];render();toast('已清空本机导入内容')}});if(S.selected)bindDetail();if(S.upload)bindUpload()}
